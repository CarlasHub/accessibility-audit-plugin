{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "accessibility-audit": {
      "type": "stdio",
      "command": "node",
      "args": ["dist/mcp.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
